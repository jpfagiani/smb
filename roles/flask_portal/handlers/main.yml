---
  - name: reload systemd
    ansible.builtin.systemd:
      daemon_reload: true

  - name: restart portal
    ansible.builtin.service:
      name: "{{ portal.nome }}"
      state: restarted

  - name: reload nginx
    ansible.builtin.service:
      name: nginx
      state: reloaded
